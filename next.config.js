/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    images: { unoptimized: true },
    basePath: "/site-acessivel-nicson",
    assetPrefix: "/site-acessivel-nicson/",
};

module.exports = nextConfig;
